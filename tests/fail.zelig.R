library(Zelig)
data(turnout)
# zelig(vote ~ race, data = turnout, model = "probit.net")
# zelig(vote ~ race, data = turnout, model = "probit.gam")
# zelig(vote ~ race, data = turnout, model = "xxxprobit.net")







